"secret-handshake.8th" f:include
needs exercism/test
with: test
11 tests

"Wink for 1"
    ( 1 commands )
    c("wink"
    equal)

SKIP-REST-OF-TESTS

"Double blink for 10"
    ( 2 commands )
    c("double blink"
    equal)

"Close your eyes for 100"
    ( 4 commands )
    c("close your eyes"
    equal)

"Jump for 1000"
    ( 8 commands )
    c("jump"
    equal)

"Combine two actions"
    expect_equal(commands(3), c("wink"
    "double blink"
    equal?)

"Reverse two actions"
    expect_equal(commands(19), c("double blink"
    "wink"
    equal?)

"Reversing one action gives the same action"
    ( 24 commands )
    c("jump"
    equal)

"Reversing no actions still gives no actions"
    commands 16 |> should be Empty

"All possible actions"
    expect_equal(commands(15), c("wink", "double blink", "close your eyes"
    "jump"
    equal?)

"Reverse all possible actions"
    expect_equal(commands(31), c("jump", "close your eyes", "double blink"
    "wink"
    equal?)

"Do nothing for zero"
    commands 0 |> should be Empty

